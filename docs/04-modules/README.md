# Contrátame! — Módulos

Esta sección documenta el objetivo, alcance, responsabilidades, reglas, datos, seguridad y trazabilidad de cada módulo funcional.

## Módulos implementados

- `MOD-01-authentication-user-profiles.md` — `MOD-01 — Autenticación y perfiles de usuario`, implementación `IMP-001`.
- `MOD-02-worker-profile-onboarding.md` — `MOD-02 — Perfil profesional y onboarding del trabajador`, implementación `IMP-003`.

Cada implementación significativa deberá enlazar su módulo con requisitos `RF-XXX`/`RNF-XXX`, pruebas `TEST-XXX` y un registro `IMP-XXX`.
