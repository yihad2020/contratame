import { AppButton } from '@/components/ui/AppButton'; import { Screen } from '@/components/ui/Screen'; import { Body, ErrorMessage, Title } from '@/components/ui/Typography'; import { useAuth } from '@/modules/auth/auth-context';
export default function AccountErrorScreen() { const { errorMessage, reloadProfile, signOut } = useAuth(); return <Screen><Title>No pudimos cargar tu cuenta</Title><Body>La identidad existe, pero el perfil no pudo resolverse correctamente. No se habilitó acceso protegido.</Body>{errorMessage ? <ErrorMessage>{errorMessage}</ErrorMessage> : null}<AppButton label="Reintentar" onPress={() => void reloadProfile()} /><AppButton label="Cerrar sesión" onPress={() => void signOut()} variant="secondary" /></Screen>; }
