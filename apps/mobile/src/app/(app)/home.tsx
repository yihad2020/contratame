import { router } from 'expo-router'; import { StyleSheet, Text, View } from 'react-native'; import { AppButton } from '@/components/ui/AppButton'; import { Screen } from '@/components/ui/Screen'; import { Body, Title } from '@/components/ui/Typography'; import { colors, spacing } from '@/constants/theme'; import { useAuth } from '@/modules/auth/auth-context';
export default function HomeScreen() { const { profile, signOut } = useAuth(); return <Screen><Text style={styles.brand}>Contrátame!</Text><Title>Hola, {profile?.first_name}</Title><Body muted>Tu cuenta está activa. Este primer módulo cubre solamente autenticación y perfil general.</Body><View style={styles.card}><Text style={styles.label}>Estado de cuenta</Text><Text style={styles.status}>Activa</Text></View><AppButton label="Ver y editar mi perfil" onPress={() => router.push('/(app)/profile')} /><AppButton label="Cerrar sesión" onPress={() => void signOut()} variant="secondary" /></Screen>; }
const styles = StyleSheet.create({ brand: { color: colors.primary, fontWeight: '900', fontSize: 20, marginTop: spacing.lg }, card: { backgroundColor: colors.surface, borderWidth: 1, borderColor: colors.border, padding: spacing.lg, borderRadius: 12, gap: spacing.sm }, label: { color: colors.muted }, status: { color: colors.green, fontWeight: '800', fontSize: 18 } });
